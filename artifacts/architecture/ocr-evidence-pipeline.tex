% Regenerate: cd artifacts/architecture && tectonic ocr-evidence-pipeline.tex && pdftocairo -svg ocr-evidence-pipeline.pdf ocr-evidence-pipeline.svg
\input{../research/figures/orx-tikz-preamble}
\usepackage{fontawesome5}

\begin{document}
\begin{tikzpicture}[orx, node distance=5mm and 5mm]

  \node[data, minimum width=28mm] (source)
    {\faFileImage\quad Source pages\\\scriptsize PDF, TIFF, image};
  \node[block, right=of source, minimum width=33mm] (prepare)
    {\faCrop\quad Page preparation\\\scriptsize render, transpose, crop guard};
  \node[block, right=of prepare, minimum width=35mm] (orient)
    {\faCompass\quad Orientation\\\scriptsize page pose + margin recovery};
  \node[block, right=of orient, minimum width=34mm] (read)
    {\faEye\quad Primary reader\\\scriptsize positioned literal words};

  \node[muted, below=8mm of prepare, minimum width=33mm] (recover)
    {\faSearchPlus\quad Bounded recovery\\\scriptsize faint, tiny, wide-band};
  \node[muted, left=of recover, minimum width=30mm] (tables)
    {\faTable\quad Tables\\\scriptsize TATR + selective cell read};
  \node[muted, right=of recover, minimum width=33mm] (controls)
    {\faCheckSquare\quad Controls\\\scriptsize state + label ownership};
  \node[muted, right=of controls, minimum width=31mm] (layout)
    {\faProjectDiagram\quad Structure\\\scriptsize hierarchy + reading order};

  \node[ours, below=9mm of recover, minimum width=86mm] (evidence)
    {\faDatabase\quad Canonical evidence document\\
     \scriptsize stable ID, literal text, true pixel geometry, provider score and scope,\\
     \scriptsize provenance, alternatives, table/control/field relationships};
  \node[block, right=of evidence, minimum width=39mm] (verify)
    {\faShield*\quad Verify and route\\\scriptsize accept, review, retained failure};

  \node[data, below=9mm of evidence, minimum width=31mm] (api)
    {\faCode\quad API JSON\\\scriptsize schema v2};
  \node[data, left=of api, minimum width=31mm] (sourceview)
    {\faVectorSquare\quad Source view\\\scriptsize semantic overlays};
  \node[data, right=of api, minimum width=34mm] (textview)
    {\faHighlighter\quad Text view\\\scriptsize inline scoped confidence};
  \node[data, right=of textview, minimum width=31mm] (exports)
    {\faFileDownload\quad Exports\\\scriptsize Markdown + JSON};

  \draw[flow] (source) -- (prepare);
  \draw[flow] (prepare) -- (orient);
  \draw[flow] (orient) -- (read);
  \draw[flow] (read.south) -- node[edgelbl, left, pos=.35] {word evidence} (layout.north);

  \draw[flow] (tables.south) |- ([yshift=-4mm]recover.south);
  \draw[flow] (recover.south) -- node[edgelbl, right] {only gated crops} (evidence.north);
  \draw[flow] (controls.south) |- ([yshift=-4mm]recover.south);
  \draw[flow] (layout.south) |- (evidence.east);
  \draw[backflow] (read.south) |- node[edgelbl, pos=.58, above] {review trigger} (recover.north);
  \draw[flow] (evidence) -- node[edgelbl, align=center, above=8mm] {validated\\relations} (verify);

  \draw[flow] (verify.south) |- (sourceview.north);
  \draw[flow] (verify.south) |- (api.north);
  \draw[flow] (verify.south) |- (textview.north);
  \draw[flow] (verify.south) |- (exports.north);

  \begin{scope}[on background layer]
    \node[stage, fit=(source)(prepare)(orient)(read)] {};
    \node[stage, fit=(tables)(recover)(controls)(layout)] {};
    \node[stage, fit=(sourceview)(api)(textview)(exports)] {};
  \end{scope}

\end{tikzpicture}
\end{document}
